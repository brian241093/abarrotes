function agregarAlCarrito(producto, precio) {
  alert(`Has agregado "${producto}" al carrito por $${precio}`);
}

function comprarAhora(producto, precio) {
  alert(`Estás comprando "${producto}" por $${precio}. ¡Gracias por tu compra!`);
}
